= parent_form.fields_for restaurant do |f|
  .form-group
    = f.label :reservation_type
    = f.select :reservation_type, restaurant.getReservationTypes.collect { |r| [r,r] }, {}, {:class => 'form-control'}
  .form-group
    = f.label :url
    = f.text_field :url, class: 'form-control'